[48. Rotate Image](https://leetcode.com/problems/rotate-image/)

# Intuition

90° clockwise rotation =

1. **Transpose** the matrix
2. **Reverse each row**

This transforms `(i, j)` → `(j, n-1-i)` without extra space.

---

# Approach

### Step 1: Transpose

Swap:

```
matrix[i][j] ↔ matrix[j][i]
```

### Step 2: Reverse each row

Reverse elements in every row

---

# Complexity

* **Time complexity:**
  $$O(n^2)$$

* **Space complexity:**
  $$O(1)$$

---

# Code

```typescript
/**
 Do not return anything, modify matrix in-place instead.
 */
function rotate(matrix: number[][]): void {
    const n = matrix.length;

    // Step 1: transpose
    for (let i = 0; i < n; i++) {
        for (let j = i + 1; j < n; j++) {
            [matrix[i][j], matrix[j][i]] = [matrix[j][i], matrix[i][j]];
        }
    }

    // Step 2: reverse each row
    for (let i = 0; i < n; i++) {
        matrix[i].reverse();
    }
}
```

---

# Example Walkthrough

## Example 1: Step by step explanation

Input:

```
[ [1,2,3],
  [4,5,6],
  [7,8,9] ]
```

Transpose:

```
[ [1,4,7],
  [2,5,8],
  [3,6,9] ]
```

Reverse rows:

```
[ [7,4,1],
  [8,5,2],
  [9,6,3] ]
```

---

## Example 2:

Same process:

* Transpose
* Reverse rows

Produces rotated matrix correctly
